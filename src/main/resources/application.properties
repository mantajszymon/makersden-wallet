spring.application.name=makersden
spring.mvc.problemdetails.enabled=true

# Swagger UI reads the hand-written spec at src/main/resources/static/openapi.yaml
# (served automatically at /openapi.yaml) instead of generating one from annotations.
springdoc.swagger-ui.url=/openapi.yaml
